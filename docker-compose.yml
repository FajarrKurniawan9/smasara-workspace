services:
  # 1. Database (PostgreSQL)
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 531269
      POSTGRES_DB: smasara_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # 2. Go Backend
  backend:
    build:
      context: ./backend
      dockerfile: dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      # Menggabungkan semua kredensial menjadi satu URL koneksi untuk Go
      - DATABASE_URL=postgres://postgres:531269@db:5432/smasara_db?sslmode=disable
      # Mempertahankan variabel individu untuk berjaga-jaga jika kode Anda membutuhkannya
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=531269
      - DB_NAME=smasara_db

  # 3. Svelte Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - backend
    environment:
      - BACKEND_URL=http://backend:8080
    # sehingga Anda tidak perlu merestart docker saat mengedit file Svelte
    volumes:
      - ./frontend:/app
      - /app/node_modules

volumes:
  # Ini memastikan data database Anda tidak hilang saat container dimatikan
  postgres_data:
